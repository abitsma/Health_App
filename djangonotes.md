For the BACK END

each individual page is an "app"
we use the django "create app" function to make an app
If we did "create app tracker"
then we'd get a tracker folder

We want to focus on the admin and views files

In Django, there's "models"
the "views" file accesses information from the "models" and displays it

We can put our CSS files in a STATIC file at the root level instead of the app level, which will solve Ben's issue
